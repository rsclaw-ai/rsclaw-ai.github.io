# rsclaw update

Auto-update from GitHub releases.

## Usage

```bash
rsclaw update
```

Downloads the latest release binary from [github.com/rsclaw-ai/rsclaw](https://github.com/rsclaw-ai/rsclaw) and replaces the current binary in-place.

If the gateway is running, it auto-restarts after update.
